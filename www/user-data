#cloud-config
preserve_hostname: false
hostname: linuxsubsystem
users:
    - name: laksith
      shell: /bin/bash
      sudo: ['ALL=(ALL) ALL']
ssh_pwauth: True
chpasswd:
    list: |
      laksith:supersecurepassword
    expire: True
